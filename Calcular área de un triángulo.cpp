#include <iostream>
using namespace std;

int main() {
    float base, altura;
    cout << "Ingrese base y altura del triangulo: ";
    cin >> base >> altura;

    cout << "Area del triangulo = " << (base * altura) / 2;

    return 0;
}
